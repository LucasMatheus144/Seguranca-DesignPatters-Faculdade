@model EspacoPotencial.Areas.Cadastro.Models.Usuarios.escolas

@{
    ViewData["Title"] = "Detalhes";
}

<h1 class="display-4 text-center mb-5">Detalhes da Escola</h1>

<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow">
                <div class="card-body">
                    <h4 class="card-title text-center mb-4">Escola</h4>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-3 text-muted">
                            <strong>Nome:</strong>
                        </dt>
                        <dd class="col-sm-9">
                            @Html.DisplayFor(model => model.Nome)
                        </dd>
                        <dt class="col-sm-3 text-muted">
                            <strong>Cidade:</strong>
                        </dt>
                        <dd class="col-sm-9">
                            @Html.DisplayFor(model => model.Cidade)
                        </dd>
                        <dt class="col-sm-3 text-muted">
                            <strong>Telefone:</strong>
                        </dt>
                        <dd class="col-sm-9">
                            @Html.DisplayFor(model => model.Telefone)
                        </dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="text-center mt-5">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a>
    <a asp-action="Index" class="btn btn-secondary ml-3"><i class="fas fa-angle-double-left"></i> Voltar</a>
</div>
