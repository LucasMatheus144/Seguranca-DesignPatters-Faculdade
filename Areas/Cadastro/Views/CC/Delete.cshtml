@model EspacoPotencial.Areas.Cadastro.Models.Financeiro.cc

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="card mt-5">
                <div class="card-body">
                    <h1 class="card-title text-center mb-5 text-danger">Confirmação de Exclusão</h1>
                    <h3 class="text-center mb-4">Tem certeza que deseja excluir este registro de conta corrente?</h3>

                    <div class="mt-4">
                        <h4 class="mb-3">Detalhes da Conta Corrente</h4>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-2 text-muted">Conta Corrente Venda Id Origem:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.ContaCorrenteVendaIdOrigem)</dd>
                            <dt class="col-sm-2 text-muted">Conta Corrente Venda Id:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.ContaCorrenteVendaId)</dd>
                            <dt class="col-sm-2 text-muted">Data:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.ContaCorrenteData)</dd>
                            <dt class="col-sm-2 text-muted">Débito/Crédito:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.ContaCorrenteDebitoCredito)</dd>
                            <dt class="col-sm-2 text-muted">Valor:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.ContaCorrenteValor)</dd>
                        </dl>

                        <div class="text-center mt-5">
                            <form asp-action="Delete">
                                <input type="hidden" asp-for="Id" />
                                <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i> Confirmar Exclusão</button>
                                <a asp-action="Index" class="btn btn-outline-secondary ml-3"><i class="fas fa-angle-double-left"></i> Voltar</a>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
