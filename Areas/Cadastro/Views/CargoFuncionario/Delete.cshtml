@model EspacoPotencial.Areas.Cadastro.Models.Funcionarios.cargo_funcionario

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="card mt-5">
                <div class="card-body">
                    <h1 class="card-title text-center mb-5 text-danger">Confirmação de Exclusão</h1>
                    <h3 class="text-center mb-4">Tem certeza que deseja excluir este cargo de funcionário?</h3>

                    <div class="mt-4">
                        <h4 class="mb-3">Detalhes do Cargo de Funcionário</h4>
                        <hr />
                        <dl class="row">
                            <dt class="col-sm-2 text-muted">Cargo:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.cargos_id)</dd>
                            <dt class="col-sm-2 text-muted">Situação:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.Situacao)</dd>
                            <dt class="col-sm-2 text-muted">Data Inicial:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.DataInicial)</dd>
                            <dt class="col-sm-2 text-muted">Data Final:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.DataFinal)</dd>
                            <dt class="col-sm-2 text-muted">Observação:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.Observacao)</dd>
                            <dt class="col-sm-2 text-muted">Motivo de Desligamento:</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.motivo_desligamento.Descricao)</dd>
                        </dl>

                        <div class="text-center mt-5">
                            <form asp-action="Delete">
                                <input type="hidden" asp-for="Id" />
                                <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i> Confirmar Exclusão</button>
                                <a asp-action="Index" class="btn btn-outline-secondary ml-3"><i class="fas fa-angle-double-left"></i> Voltar</a>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
