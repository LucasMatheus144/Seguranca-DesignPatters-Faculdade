@model EspacoPotencial.Areas.Cadastro.Models.Financeiro.produtos_evento

@{
    ViewData["Title"] = "Create";
}

<h1>Cadastro</h1>

<h4>produtos_evento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="eventos_id" class="control-label"></label>
                <select asp-for="eventos_id" class ="form-control" asp-items="ViewBag.eventos_id"></select>
            </div>
            <div class="form-group">
                <label asp-for="grp_id" class="control-label"></label>
                <select asp-for="grp_id" class ="form-control" asp-items="ViewBag.grp_id"></select>
            </div>
            <div class="form-group">
                <label asp-for="produtos_id" class="control-label"></label>
                <select asp-for="produtos_id" class ="form-control" asp-items="ViewBag.produtos_id"></select>
            </div>
            <div class="form-group">
                <label asp-for="prod_evento_valor_uni" class="control-label"></label>
                <input asp-for="prod_evento_valor_uni" class="form-control" type="text">
                <span asp-validation-for="prod_evento_valor_uni" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                 <a asp-action="Index" class="btn btn-secondary "><i class="fas fa-angle-double-left"></i> Voltar</a>
            </div>
        </form>
    </div>
</div>

>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
