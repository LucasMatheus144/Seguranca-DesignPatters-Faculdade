@model EspacoPotencial.Areas.Cadastro.Models.Usuarios.comorbidade_usuario

@{
    ViewData["Title"] = "Editar";
}

<h1>Edilçao</h1>

<h4>comorbidade_usuario</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="usuario_id" class="control-label"></label>
                <select asp-for="usuario_id" class="form-control" asp-items="ViewBag.usuario_id"></select>
                <span asp-validation-for="usuario_id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="comorbidade_id" class="control-label"></label>
                <select asp-for="comorbidade_id" class="form-control" asp-items="ViewBag.comorbidade_id"></select>
                <span asp-validation-for="comorbidade_id" class="text-danger"></span>
            </div>
            <br>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary "><i class="fas fa-angle-double-left"></i> Voltar para a listagem</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
